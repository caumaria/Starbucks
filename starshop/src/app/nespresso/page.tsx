'use client';
import styled from 'styled-components';

const NespressoContainer = styled.section`
  margin-top: 8rem;
`;

export default function Nespresso () {
  return (
    <NespressoContainer>
      nespresso
    </NespressoContainer>
  );
}