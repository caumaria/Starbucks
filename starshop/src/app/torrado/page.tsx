'use client';
import styled from 'styled-components';

const TorradoContainer = styled.section`
  margin-top: 8rem;
`;

export default function Torrado () {
  return (
    <TorradoContainer>
      torrado
    </TorradoContainer>
  );
}